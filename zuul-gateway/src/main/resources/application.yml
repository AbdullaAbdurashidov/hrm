server:
  port: 8888

spring:
  application:
    name: zuul-gateway

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8700/eureka/
